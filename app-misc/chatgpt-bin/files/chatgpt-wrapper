#!/bin/sh
# The app ignores --ozone-platform-hint and starts on XWayland; force
# native Wayland when a compositor is present, with text-input IME.
if [ -n "${WAYLAND_DISPLAY}" ]; then
	set -- --ozone-platform=wayland --enable-wayland-ime "$@"
fi
exec /opt/chatgpt/codex-launcher "$@"
